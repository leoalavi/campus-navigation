# Miscellaneous
build/
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# macOS build artifacts (Xcode cache — filenames too long for Windows)
macos/build/

# Generated localisation files (regenerated by flutter gen-l10n)
lib/app/l10n/generated/

# Binary documents
*.docx

# JVM/Android heap dumps (generated by Gradle on OOM)
*.hprof

# Environment files
.env
.env.*
!.env.example
web/google_maps_config.js
ios/Flutter/Secrets.xcconfig

# Firebase config (contains project-specific keys)
google-services.json
GoogleService-Info.plist

# Android secrets
android/secrets.properties

# Flutter plugins file
.flutter-plugins

# Codex desktop workspace metadata
.codex/
logs/

# AI superpowers / plan artifacts
.superpowers/
docs/superpowers/

# Antigravity CLI workspace metadata
.antigravitycli/


# Web is kept BUILDABLE so CI can compile and test it, but Campus Navigation
# is not released as a web app: the shipped product is Android + iOS, and
# Syllabus Sync hands off to the installed app or offers the store listings.
# `web/` is therefore tracked (not ignored) — it just never gets deployed.
